{
  "builder": "NIXPACKS",
  "buildCommand": "npx tsc --project tsconfig.voice-worker.json --noEmit",
  "startCommand": "npm run start:voice-worker",
  "healthcheckPath": "/health",
  "healthcheckTimeout": 120,
  "restartPolicyType": "ALWAYS",
  "numReplicas": 1,
  "sleepApplication": false
}
